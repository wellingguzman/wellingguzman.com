- var tagsData = public.tags._data;
- tagList = tagsData ? Object.keys(tagsData) : [];
- return tagList.filter(function (key) {
-  var tag = tagsData[key];
-  return key === 'index' ? false : tag.hidden !== true;
- }).sort(function (a, b) {
-   return a.localeCompare(b);
- }).map(function (key) {
-   var tag = tagsData[key];
-   tag.name = key;
-   tag.path = '/tags/' + key;
-   tag.url = site_url + tag.path;
-   return tag;
- });
