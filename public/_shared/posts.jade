- var blog = public.writing || public.public.writing;
- // For some reason I need to take the index out of the list
- // It return a all wrong sorted list.
- var posts = Object.keys(blog._data);
- var indexPageIndex = posts.indexOf('index');
- if (indexPageIndex>-1) posts.splice(indexPageIndex,1);
- return posts.map(function (slug) {
-   var post = blog._data[slug];
-   post.slug = slug;
-   post.url = site_url + '/writing/' + post.slug;
-   post.url_relative = '/writing/' + post.slug;
-   return post;
- }).sort(function(a,b){
-   if (!a.date) return 0;
-   a = +new Date(a.date);
-   b = +new Date(b.date);
-   return a-b;
- }).reverse().filter(function (post) {
-   if (environment == "production" && post.published===false) {
-     return false;
-   }
-   if (filter) {
-      return filter(post);
-   }
-   return post;
- }).slice(0, postlimit)