- var blog = public.writing || public.public.writing;
- return Object.keys(blog._data).map(function (slug) {
-   var post = blog._data[slug];
-   post.slug = slug;
-   post.url = site_url + '/writing/' + post.slug;
-   post.url_relative = '/writing/' + post.slug;
-   return post;
- }).sort(function(a,b){
-   if (!a.date) return 0;
-   a = +new Date(a.date);
-   b = +new Date(b.date);
-   return a-b;
- }).reverse().filter(function (post) {
-   if (environment == "production" && post.published===false) {
-     return false;
-   }
-   if (filter) {
-      return filter(post);
-   }
-   return post;
- }).slice(0, postlimit)